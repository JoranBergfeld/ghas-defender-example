# Seeded Vulnerabilities

Populated in Plan 7.

Every seeded vulnerability must be present only on the `vulnerable` branch, absent from `secure`, and labelled in code with `// SEEDED VULN #N — see scripts/seed-vulnerabilities.md` or the equivalent `#` comment marker for YAML, Dockerfile, and Bicep.

| # | Category | Location | Concrete seed | Expected control |
| --- | --- | --- | --- | --- |
